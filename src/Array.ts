let numbers: number[] = [];
// numbers[0]= 1;
// numbers[1] = 2;
// numbers[2] = '3' you can't assign a string

// the one of benefit its. when you loop this array it 
numbers.forEach(n => n.toString)

// here you find that all thte method of number object, ypu do not get it in javascript