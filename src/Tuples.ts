const user: [number, string] = [1, 'Rahat']
user[1]
// here you can find all the method of string number or a array. its the another reason why public love typescript.
// but tupels is suitable for only two value like, key && value